<%= form_for @new_doc, html: {multipart: true} do |f| %>
	<%= f.label :ref %>
	<%= f.text_field :ref %>
	<%= f.label :status %>
	<%= f.text_field :status %>
	<%= f.label :pic %>
	<%= f.file_field :pic %>
	<%= f.submit 'Upload Doc' %>
<% end %>

